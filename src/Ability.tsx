import {
  FormControl,
  FormHelperText,
  FormLabel,
  Input,
} from "@chakra-ui/react";

function Ability() {
  return (
    <FormControl>
      <Input type="text" />
    </FormControl>
  );
}

export default Ability;
